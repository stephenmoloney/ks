1. Get the frontend application URL by running these commands:
{{- if .Values.common.ingress.enabled }}
  http{{ if $.Values.common.ingress.tls }}s{{ end }}://{{ . }}{{ $.Values.frontend.ingress.path }}
{{- else if contains "ClusterIP" .Values.common.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ template "ks3.name" . }},release={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl port-forward $POD_NAME 8080:80
{{- end }}
